1、LocalDate类

- now()方法
  - 用于从默认时区中的系统时钟获取当前日期，此方法将基于具有默认时区的系统时钟返回LocalDate以获取当前日期。
  - 代码实现

```java
import java.time.*;

public class GFG {
  public static void main(String[] args) {

    // create an LocalDate object

    LocalDate lt = LocalDate.now();

    // print result
    System.out.println("LocalDate : " + lt);
  }
}
```

输出：

LocalDate : 2019-01-21

- getYear()方法
  - 该方法的返回类型为int，它返回year字段的值
  - 代码实现

```java
import java.time.*;

public class GetYearOfLocalDate {

  public static void main(String args[]) {

    // Instantiates two LocalDate

    LocalDate l_da1 = LocalDate.parse("2007-04-04");

    LocalDate l_da2 = LocalDate.now();

    // Display l_da1,l_da2
    System.out.println("LocalDate l_da1 and l_da2: ");
    System.out.println("l_da1: " + l_da1);
    System.out.println("l_da2: " + l_da2);
    System.out.println();

    // Here, this method gets the
    // value for the field YEAR from
    // this date l_da1

    int get_val = l_da1.getYear();

    // Display get_val
    System.out.println("l_da1.getYear(): " + get_val);

    // Here, this method gets the
    // value for the field YEAR from
    // this date l_da2

    get_val = l_da2.getYear();

    // Display get_val
    System.out.println("l_da2.getYear(): " + get_val);
  }
}
```

输出：

LocalDate l_da1 and l_da2:

l_da1: 2007-04-04

l_da2: 2020-05-30

l_da1.getYear(): 2007

l_da2.getYear(): 2020

- getDayOfMonth()方法
  - 在java.time包中可用。用于获取此LocalDate的月日字段值。是一个非静态方法，只能通过类对象访问，如果尝试使用类名称访问该方法，则会收到错误消息。在获取每月的某天时不会引发异常。
  - 此方法的返回类型为int ，它返回此LocalDate的字段“ day-of-month”的值，并且一个月中的天数从1到31开始。返回今天在这个月是第几天
  - 代码实现

```java
import java.time.*;

public class GetDayOfMonthOfLocalDate {

    public static void main(String args[]) {

        // Instantiates two LocalDate
```

LocalDate l_da1 = LocalDate.parse("2007-04-05");

LocalDate l_da2 = LocalDate.now();

```java
        // Display l_da1,l_da2
        System.out.println("LocalDate l_da1 and l_da2: ");
        System.out.println("l_da1: " + l_da1);
        System.out.println("l_da2: " + l_da2);
        System.out.println();

        // Here, this method gets the value of
        // the field day-of-month in this
        // date object l_da1
```

int dom = l_da1.getDayOfMonth();

```java
        // Display dom
        System.out.println("l_da1.getDayOfMonth(): " + dom);

        // Here, this method gets the value of
        // the field day-of-month in this
        // date object l_da2
```

dom = l_da2.getDayOfMonth();

```java
        // Display dom
        System.out.println("l_da2.getDayOfMonth(): " + dom);
    }
}
```

输出：

LocalDate l_da1 and l_da2:

l_da1: 2007-04-05

l_da2: 2020-05-29

l_da1.getDayOfMonth(): 5

l_da2.getDayOfMonth(): 29

- getMonthValue()方法
  - 获取一年中的月份，获取localDate中的月份。此方法不需要任何参数，它返回一年中的月份，范围在1到12之间。
  - 代码实现

```java
import java.time.*;

public class Main {
    public static void main(String[] args) {
```

LocalDate ld = LocalDate.parse("2019-02-14");

```java
        System.out.println("The LocalDate is: " + ld);
        System.out.println("The month is: " + ld.getMonthValue());
    }
}
```

输出：

The LocalDate is: 2019-02-14

- plusMonths()方法
  - 此方法的返回类型为LocalDate ，它返回LocalDate，该LocalDate包含将给定月份添加到此LocalDate的值。
  - 代码实现

```java
import java.time.*;

public class PlusMonthsOfLocalDate {

    public static void main(String args[]) {
```

long months = 5;

```text
        // Instantiates two LocalDate
```

LocalDate l_da1 = LocalDate.parse("2007-04-04");

LocalDate l_da2 = LocalDate.of(2008, Month.FEBRUARY, 06);

```java
        // Display l_da1,l_da2 and months
        System.out.println("LocalDate l_da1,l_da2 : ");
        System.out.println("l_da1: " + l_da1);
        System.out.println("l_da2: " + l_da2);
        System.out.println("months to add: " + months);
        System.out.println();

        // Here, this method adds the given
        // months to this date l_da1
        // i.e. here we are adding 5
        // months to the date l_da1
```

LocalDate plus_mon = l_da1.plusMonths(months);

```java
        // Display plus_mon
        System.out.println("l_da1.plusMonths(months): " + plus_mon);

        // Here, this method adds the given
        // months to this date l_da2
        // i.e. here we are adding 5
        // months to the date l_da2
```

plus_mon = l_da2.plusMonths(months);

```java
        // Display plus_mon
        System.out.println("l_da2.plusMonths(months): " + plus_mon);
    }
}
```

输出：

LocalDate l_da1,l_da2 :

l_da1: 2007-04-04

l_da2: 2008-02-06

months to add: 5

l_da1.plusMonths(months): 2007-09-04

l_da2.plusMonths(months): 2008-07-06

- lengthOfMonth()方法
  - 此方法的返回类型为int ，它返回此对象表示的月份的长度。
  - 代码实现

```java
import java.time.*;

public class LengthOfMonthOfLocalDate {

    public static void main(String args[]) {

        // Instantiates three LocalDate
```

LocalDate l_da1 = LocalDate.parse("2007-04-04");

```java
        // Display l_da1,l_da2 and l_da3
        System.out.println("l_da1: " + l_da1);

        // Here, this method returns the
        // length of month represented by
        // this date l_da1 i.e. here
        // l_da1 = "2007-04-04" then number
        // of days in the month of april is
        // 30 i.e. length = 30
```

int length = l_da1.lengthOfMonth();

```java
        // Display length
        System.out.println("l_da1.lengthOfMonth(): " + length);
    }
}
```

输出：

LocalDate l_da1:

l_da1: 2007-04-04

l_da1.lengthOfMonth(): 30

- withDayOfMonth()方法
  - 此方法的返回类型为LocalDate ，它返回LocalDate，该LocalDate包含用给定的月日更新该对象的月日的值。
  - 代码实现

```java
import java.time.*;

public class WithDayOfMonthOfLocalDate {

    public static void main(String args[]) {
```

int day_of_month = 5;

```text
        // Instantiates two LocalDate
```

LocalDate l_da1 = LocalDate.parse("2007-04-04");

LocalDate l_da2 = LocalDate.of(2008, Month.FEBRUARY, 06);

```sql
        // Display l_da1,l_da2
        System.out.println("LocalDate l_da1,l_da2 : ");
        System.out.println("l_da1: " + l_da1);
        System.out.println("l_da2: " + l_da2);
        System.out.println("day_of_month to update: " + day_of_month);
        System.out.println();

        // Here, this method updates day-of-month
        // with the given value in this LocalDate
        // l_da1
```

LocalDate l_date = l_da1.withDayOfMonth(day_of_month);

```java
        // Display l_date
        System.out.println("l_da1.withDayOfMonth(day_of_month): " + l_date);

        // Here, this method updates day-of-month
        // with the given day-of-month in this
        // LocalDate l_da2
```

l_date = l_da2.withDayOfMonth(day_of_month);

```java
        // Display l_date
        System.out.println("l_da2.withDayOfMonth(day_of_month): " + l_date);
    }
}
```

输出:

LocalDate l_da1,l_da2 :

l_da1: 2007-04-04

l_da2: 2008-02-06

day_of_month to update: 5

l_da1.withDayOfMonth(day_of_month): 2007-04-05

l_da2.withDayOfMonth(day_of_month): 2008-02-05
