# 1 一、基础部分

- spark提交作业的参数
  - executor-cores —— 每个executor使用的内核数，默认为1，官方建议2-5个，我们企业是4个
  - num-executors —— 启动executors的数量，默认为2
  - executor-memory —— executor内存大小，默认1G
  - driver-cores —— driver使用内核数，默认为1
  - driver-memory —— driver内存大小，默认512M
- 如何理解srapk中血统（RDD）的概念，他的作用是什么
  - 概念：RDD是弹性分布式数据集，是Spark中最基本的数据抽象，代表一个不可变，可分区、里面的元素可并行计算的集合。
  - 作用： 提供了一个抽象的数据模型，将具体的应用逻辑表达为一系列转换操作（函数）。另外不同RDD之间的转换操作之间还可以形成依赖关系，进而实现管道化，从而避免了中间结果的存储，大大降低了数据复制、磁盘IO和序列化开销，并且还提供了更多的API(map/filter/groupBy…)。
  - RDD在Lineage依赖方面分为两种Narrow Dependencies与Wide Dependencies，用于解决数据容错时的高效性以及划分任务时候起到重要作用。
# 2 二、Spark面试题

- 解决数据倾斜问题：
  - 1).先说一个笨方法，抽样统计key的个数，然后将倾斜的过滤掉
  - 2).对聚合类算子进行两次操作，第一次给key加上一个随机数，然后聚合一次，第二系将加上的随机数取消掉再聚合一次
  - 3).将reduce Join转换成 map join，将小表广播出去，然后再大表操作时，使用map算子获取数据进行合并
    - 两个大表一个加扩容，一个加随机前缀将数据分散出去
  - 4).核心思想就是众人拾柴火焰高，将key均匀分布到不同的分区汇总，并行的去处理这批数据
- DAG图能看到哪些信息
  - 1).创建rdd的时候构建DAG图，一个行动算子Action创建一个DAG图，也就是一个job，一个应用中有多少个DAG就取决于有多少个行动算子
  - 2).一个DAG图中会有不同阶段也就是Stage，划分Stage的依据宽依赖，一个父RDD被多个子RDD依赖就是宽依赖，一个父RDD只被一个子RDD依赖就是窄依赖
  - 3).一个Stage中有一个Taskset，一个分区对应一个task
- TaskScheduler的作用是啥
  - 1).Stage也就是taskSet发送给taskScheduler
  - 2).taskScheduler将task发送给Executor执行
- Spark的内存分布
  - 1).堆内内存
    - 在使用堆内内存的时候，如果我们设置的堆内内存是2个G，读取的数据也是2个G，然后又来了2个G的数据
    - 这样就会出现OOM内存溢出的情况，因为你处理完2G的数据，空间并不会马上进行GC回收
  - 2).堆外内存
    - 这样我们就可以使用对外内存，也就是物理内存，堆外内存可以精准的申请和释放空间，不需要GC，性能比较高，提升了任务执行的效率
- Spark处理小文件
  - 1).coalesce和repartition调整分区
    - coalesce方法默认情况下不会将分区的数据打乱重新组合，这种情况下的缩减分区可能会导致数据不均衡，出现数据倾斜，如果想要让数据均衡，可以进行 shuffle 处理
    - 采用coalesce(num)对数据集repatition来合理的设置分区数
- Spark如何实现报警功能
  - 1).写代码去实现
    - 使用YARNClient类获取spark任务的状态
    - 判断任务的存活状态
    - 还可以去写代码获取executor的个数，cpu，内存等信息
    - 拿到我们想要的数据之后，就可以对spark进行一个状态监控的功能
- ReduceByKey和GroupByKey的区别
  - 1).reduceBykey，按照key进行聚合，在shuffle之前有combine（预聚合）的操作，返回的结果是RDD[K,V]
  - 2).groupByKey，按照key进行分组直接进行shuffle
  - 建议使用reduceBykey，但是注意的是否会影响最终的业务逻辑
- Spark的缓存
  - 1).将频繁使用的RDD进行缓存到内存中，后面的RDD用到的时候直接去内存中读取而不需要重新计算了，提交了任务执行的效率
  - 2).cache是将数据保存在内存中
  - 3).程序结束后会被清除或者是手动调用unpersist方法清除
  - 4).会记录RDD的血缘关系
- 什么是RDD
  - 1).是一个弹性分布式的数据集
  - 2).数据可以保存到内存中也可以保存到磁盘中
  - 3).他是分布式的计算的一个数据集合
  - 4).RDD有三个特征，分区、不可变、并行操作
  - 5).RDD是由好多分区组成的，操作RDD的时候，对RDD里面的每一个分区进行操作
  - 6).RDD使用算子进行操作
  - 7).算子又分为转换算子和行动算子
- Spark的落盘场景
  - 1).在shuffle中会进行落盘操作
  - 2).shuffle分为shuffle write和shuffle read
  - 3).在这期间会进行一个数据落盘的操作
- Spark的Shuffle
  - 1).spark中会导致shuffle操作的有以下几种算子、
    - 1、repartition类的操作：比如repartition、repartitionAndSortWithinPartitions、coalesce(默认不会，第二个参数shuffle:（Boolean ）：为Ture时会；少分区向多分区转换会)等
    - 2、byKey类的操作：比如reduceByKey、groupByKey、sortByKey等
    - 3、join类的操作：比如join、cogroup等
    - 重分区: 一般会shuffle，因为需要在整个集群中，对之前所有的分区的数据进行随机，均匀的打乱，然后把数据放入下游新的指定数量的分区内
    - byKey类的操作：因为你要对一个key，进行聚合操作，那么肯定要保证集群中，所有节点上的，相同的key，一定是到同一个节点上进行处理
    - join类的操作：两个rdd进行join，就必须将相同join
    - key的数据，shuffle到同一个节点上，然后进行相同key的两个rdd数据的笛卡尔乘积
  - 2).hashshuffle在2.0之后就不用了
    - hashshuffle
      - 优化前下游有一个task他就会生成一个对应的文件，有几个task就会有几个文件
      - 优化后通过复用buffer来优化shuffle过程中产生小文件的数量
    - sortshuffle
      - 当shuffle read task的数量小于等于默认的200个时，并且不是聚合类的shuffle算子，就会启动bypass机制，bypass机制并没有对数据进行sort
- Spark内存溢出OOM的情况
  - 1).map过程产生大量对象大致内存溢出
  - 2).数据不平衡导致内存溢出
  - 3).coalesce调用导致内存溢出
  - 4).shuffle后内存溢出
  - 5).standalone模式下资源分配不均匀导致内存溢出
  - 6).在RDD中，共用对象能够减少OOM的情况
- 怎样避免SparkOOM
  - 1).使用mapPartitions代替大部分map操作，或者连续使用的map操作
  - 2).broadcast join（map join）和普通join
  - 3).先filter在join先where在join
  - 4).partitionBy优化分区优化
  - 5).combineByKey的使用
  - 6).参数优化
- Spak Shuffle的默认并行度
  - 参数spark.sql.shuffle.partitions 决定 默认并行度200
- Coalesce和Repatition的区别
  - Coalesce和Repatition都用于改变分区，Coalesce用于缩小分区不进行shuffle，Repatition用于增大分区（提供并行度）会进行shuffle在spark中减少文件个数会使用Coalesce来减少分区来达到这个目的，但是如果数据量过大，分区数过少会出现OOM所以使用Coalesce缩小分区个数也需合理
- 如何使用Spark获取TopN
  - 1).方法1
    - 按照key对数据进行聚合（groupByKey）
    - 将value转换为数组，利用scala的sortBy或者sortWith进行排序（mapValues）数据量太大，会OOM。
  - 2).方法2
    - 自定义分区器，按照key进行分区，使不同的key进到不同的分区
    - 对每个分区运用spark的排序算子进行排序
- Spark中的共享变量
  - 1).累加器（accumulator）：是Spark中提供的一种分布式的变量机制，其原理类似于mapreduce，先分后合。累加器的一个常见用途就是在调试时对作业执行过程中的事件进行计数。
  - 2).广播变量：用来高效分发较大的对象
- Coalesce和Repatition的关系与区别
  - 两者都是用来改变RDD的partition数量的，Repatition底层调用的就是coalesce方法：coalesce（numPartitions，shuffle=true）
  - 区别：Repatition一定会发生shuffle，coalesce根据传入的参数来判断是否发生shuffle
  - 一般情况下增大rdd的partition数量使用Repatition，减少partition数量时使用Coalesce
- Yarn-client提交流程
- Yarn-cluster提交流程
- Spark调优
  - 原则一：避免创建重复的RDD
  - 原则二：尽可能复用同一个RDD
  - 原则三：对多次使用的RDD进行持久化
  - 原则四：尽量避免使用shuffle类算子
  - 原则五：使用map-side预聚合的shuffle操作
  - 原则六：使用高性能算子
  - 原则七：广播大变量
  - 原则八：使用Kryo优化序列化性能
    - kryo序列化比Java序列化更紧凑，但Spark默认的序列化是java序列化而不是Spark序列化
    - 因为Spark序列化并不支持素有序列化类型，而且每次使用都必须进行注册
    - 注册只针对于RDD，在DataFrames和DataSet当中自动实现kryo序列化
  - 原则九：优化数据结构
    - 在java中有三种类型比较耗费内存
      - 对象（有对象头，引用等额外信息）
      - 字符串（每个字符串都有一个字符数组及长度等额外信息）
      - 集合类型，比如hashMap，linkedList等，因为集合类型内部会用一些内部类来封装集合元素
    - 尽量少使用上述的三种数据类型可以起到优化，但是后期代码维护会比较麻烦
- 提交spark任务到yarn设置了8个G的executor内存为6个G，结果消耗了7个G的资源
  - 会设置超过的6个G+1024的归整化因子内存（默认是1G）=5G内存
  - 合理的去根据源码调整申请内存的大小，申请多了会造成一个资源的浪费，比如我就申请的6个G结果抛出了近7个G的资源消耗，管理集群的哥们又该找上门来了