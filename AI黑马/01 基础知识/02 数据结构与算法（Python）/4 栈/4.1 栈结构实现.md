## 栈结构实现
栈可以用顺序表实现，也可以用链表实现。

### 栈的操作
+ Stack() 创建一个新的空栈
+ push(item) 添加一个新的元素item到栈顶
+ pop() 弹出栈顶元素
+ peek() 返回栈顶元素
+ is_empty() 判断栈是否为空
+ size() 返回栈的元素个数

### 列表实现栈
+ 列表实现栈分析

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730457976640-5f3df684-5099-4908-b349-2cbfc5837f0c.png)

> 列表两端都可以增加和删除数据,所以需要,而栈只需要在一端进行添加和删除数据
>
> 应该选择时间复杂度低的一端实现
>

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730457976706-3e476366-a54e-4a29-aee2-be999cefb0ef.png)<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730457976715-e8c68e03-bcbc-424a-b62d-71523252cf7f.png)

**头部插入删除数据**: insert(0,item)时间复杂度: O(n) pop(0)时间复杂度: O(n) **尾部插入删除数据**: append(item)时间复杂度: O(1) pop()时间复杂度: O(1)

综合看**尾部插入和删除数据时间复杂度更低**.

+ 代码实现

```plain
class Stack(object):
    """栈"""
    def __init__(self):
         self.items = []

    def is_empty(self):
        """判断是否为空"""
        return self.items == []

    def push(self, item):
        """加入元素"""
        self.items.append(item)

    def pop(self):
        """弹出元素"""
        return self.items.pop()

    def peek(self):
        """返回栈顶元素"""
        return self.items[len(self.items)-1]

    def size(self):
        """返回栈的大小"""
        return len(self.items)

if __name__ == "__main__":
    stack = Stack()
    stack.push("hello")
    stack.push("world")
    stack.push("itcast")
    print stack.size()
    print stack.peek()
    print stack.pop()
    print stack.pop()
    print stack.pop()
```

执行过程如下：

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/gif/25755350/1730457977202-8a2caa13-9a2c-46d8-b6d4-9fa7bb7a7755.gif)

### 链表实现栈
> 对于链表来说,由于保存了头部指针,所以头部插入和删除时间复杂度都为O(1)
>
> 尾部由于需要先查找到尾部,那插入和删除时间复杂度为O(n)
>
> 所以选择头部插入和删除
>

+ 代码实现

```plain
class Node:
    def __init__(self, data, next=None):
        self.data = data
        self.next = next

class LinkedStack:
    def __init__(self):
        # 最上面的元素
        self.top = None

    def push(self, value: int):
        newtop = Node(value)
        newtop.next = self.top
        self.top = newtop

    def pop(self):
        if self.top:
            value = self.top.data
            self.top = self.top.next
            return value
```

  


> 来自: [栈结构实现 | 数据结构与算法（Python）](http://127.0.0.1:5500/chapter4/section1.html)
>

