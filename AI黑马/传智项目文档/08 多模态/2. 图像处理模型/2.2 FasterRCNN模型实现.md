## 视觉模态特征提取
### 学习目标
+ 掌握基于目标检测的视觉特征处理.
+ 理解视觉单元提取方法比对.
+ 了解Visual Genome 数据集介绍.
+ 了解处理仇恨言论检测图片.

---

### 视觉单元提取方法比对
+ 卷积神经网络是目前比较通用的图像特征提取的方法, 目前大部分的图像任务大多基于一个效果较好的卷积网络比如ResNet-101提取图像表征然后在一个具体任务上进行应用. BERT在处理文本任务时其输入的信息都是词或者字, 是一个小的语义单元, 将整张图片向量作为输入将无法很好的学习视觉语义单元信息, 所以一般对图片进行目标检测操作, 然后将检测后的结果进行处理然后作为一个语义单元作为输入. 下表中展示Unicode-VL模型在句子检索和图像检索任务中使用ResNet模型和使用FasterRCNN提取检测框的差别.

---

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544066509-601206fe-f8a2-4eaf-a70c-564d2f22a94e.png)

---

+ 从图中可以看出使用CNN需要使用比R-CNN更多的特征, 而且很多特征往往是无用的. R-CNN的目标检测方法, 首先针对图像获取兴趣区域, 然后对每个兴趣区域应用目标检测器, 这样就可以准确的获得图像类别; 而CNN方法需要输入整幅图像, 而且用于大样本分类的网络往往很复杂, 计算量更大. 另外, FasterRCNN对之前几代R-CNN方法进行改进, 实现了只输入一次就可以识别所有对象的能力, 极大的提高的处理效率.

---

> + 结论: 使用目标检测提取图像的36个框或者100个框, 然后将其作为视觉语义单元进行输入, 其输出的表征质量远远高于单纯的使用ResNet进行像素级特征提取所产生的表征质量.
>

---

---

### Visual Genome 数据集介绍
#### 数据集的构成
+ Visual Genome数据集由斯坦福人工智能实验室的李飞飞教授提出, 其目的是构建一个包含丰富语义信息的视觉数据集, 整个数据集大约包含10.8万张图片, 平均每张图片含有21个物体, 16个属性, 同时其还标示了两个物体之间的关系, 也就是该数据集同时包含实体, 实体属性和实体之间的关系等三种. VG数据集主要由4个部分组成:
+ **Region Description**: 图片被划分成一个个region, 每个region都有与其对应的一句自然语言描述.

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544066036-fbd7e1ef-aa63-4c48-8429-3416e9c28c6f.png)

---

+ **Region Graph**: 每个region中的object, attribute, relationship被提取出来, 构成局部的“Scene Graph”.

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544065959-5acc88a3-d939-4f50-af52-0b17a77f8382.png)

---

+ **Scene Graph**: 把一张图片中的所有Region Graph合并成一个全局的Scene Graph.

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544066231-9b195445-19ef-407c-a37b-8a695b9aa351.png)

---

+ **QA**: 每张图片会有多对QA, 分为两种类型: region-based 和freeform, 前者基于局部描述提出, 与局部区域的内容直接相关; 后者则基于整张图片来提出.

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544065969-d0e2646e-b44e-41d1-b089-4d92cc1547b3.png)

---

#### 基于Visual Genome的目标检测模型
+ 将目标检测应用多模态最早在Bottom-Up and Top-Down Attention for Image Captioningand Visual Question Answering一文中提出. 其主要训练步骤为:
+ 命令行执行:
    - 命令执行路径: /home/multimodal/ 

```plain
# step 1: 下载代码包
git clone https://gitlab.com/vedanuj/vqa-maskrcnn-benchmark.git

# step 2: 安装命令
python setup install

# step 3: 如果遇到需要升级gcc 编译器, 将编译器升级即可.
```

---

+ 数据类别: 从2000类实体和500类属性中选出的1600类实体和400类属性, 这种方式训练的目标检测模型作为图片的特征提取器可以有效的提取图片中的视觉语义特征.
    - 目前每一张图片输出的检测框一般包含36个框或者100个框, 如上表4所示, 输出100个框的效果相对更好一些. 
    - 在Unicoder-VL中将每个检测到的对象的位置坐标表示为由归一化的左下和右上坐标组成四维向量, 其包含了位置和大小信息, 最后我们将位置向量和检测出的目标框中的特征向量进行相加, 然后通过线性映射的方式将其映射成另外的向量且其维度与文本输入向量的维度相同从而可以进行拼接.
    - VL-BERT同样将每个目标区域坐标表示成一个四维的向量, 不同的是其不是简单的直接与目标区域特征向量拼接, 而是利用与BERT位置向量相同的处理方式通过一个正弦和余弦方程映射成一个2048维的向量, 这样相当于产生了图像维度的位置向量.
    - ViLBERT也是将输出的位置向量和检测框中的特征向量进行求和, 区别是其位置向量为一个五维的向量, 除了归一化的左上和右下坐标外还增加了一个区域占比(目标区域面积占整张图片面积的比重).
    - UNINTER中则采用一个七维的位置向量(归一化的四个位置坐标, 长度, 高度, 区域面积).

---

---

### 处理仇恨言论检测的图片
+ 准备基于VG训练的目标检测模型文件与待处理的图片, 解压所有的json文件和VG_100K文件放入VGdata目录

#### 第一步: 下载vqa-mask-rcnn-benchmark 并且安装
```plain
git clone https://gitlab.com/vedanuj/vqa-maskrcnn-benchmark.git
```

---

#### 第二步: Mask-rcnn进行模型训练
```plain
# 这里的训练过程建议慎重, 因为涉及到Linux环境底层的库, 包, C++编译器的兼容问题.
# 建议同学们直接用发给大家已经训练好的模型, 直接进行检测即可.

python  ./tools/train_net.py  "configs/visual_genome_vqa/e2e_faster_rcnn_X-152-32x8d-FPN_1x_MLP_2048_FPN_512_train.yaml"
```

---

#### 第三步: 导入工具包
```plain
import argparse
import os
import sys
import json
import cv2
import numpy as np
import torch
from maskrcnn_benchmark.config import cfg
from maskrcnn_benchmark.layers import nms
from maskrcnn_benchmark.modeling.detector import build_detection_model
from maskrcnn_benchmark.structures.image_list import to_image_list
from maskrcnn_benchmark.utils.model_serialization import load_state_dict
from mmf.utils.download import download
from tools.scripts.features.extraction_utils import chunks, get_image_files
from PIL import Image, ImageDraw, ImageFont

FONT_SIZE = 13*2

COLOR_LIST = ["red", "green", "blue", "cyan", "yellow", "purple",
              "deeppink", "ghostwhite", "darkcyan", "olive",
              "orange", "orangered", "darkgreen"]
```

---

+ 定义FeatureExtractor处理类共分10个子步骤完成:
    - 第1步: 获取当前设备信息的函数 get_current_device().
    - 第2步: 初始化模块和模型加载函数 init()和_build_detection_model().
    - 第3步: 参数解析函数 get_parser().
    - 第4步: 图像处理函数 _image_transform().
    - 第5步: 特征提取函数 _process_feature_extraction().
    - 第6步: 映射字典函数 _category_id_convert_to_name().
    - 第7步: 主要处理函数 mainprocess().
    - 第8步: 检测特征函数 get_detectron_features().
    - 第9步: 存储图像特征函数 _save_feature().
    - 第10步: 提取特征函数 extract_features().

---

+ FeatureExtractor处理类的代码文件路径: /home/multimodal/

---

+ 第1步: 获取当前设备信息的函数 get_current_device().

```plain
# 获取当前设备信息
def get_current_device():
    if torch.cuda.is_available():
        return f"cuda:{torch.cuda.current_device()}"
    else:
        return torch.device("cpu")
```

---

+ 第2步: 初始化模块和模型加载函数 init()和_build_detection_model().

```plain
class FeatureExtractor:
    MAX_SIZE = 1333
    MIN_SIZE = 800


    # 特征初始化模块
    def __init__(self):
        self.args = self.get_parser().parse_args()
        self.detection_model = self._build_detection_model()
        os.makedirs(self.args.output_folder, exist_ok=True)

     # 配置文件以及模型加载
    def _build_detection_model(self):
        # 加载配置文件
        cfg.merge_from_file(self.args.config_file)
        cfg.freeze()
        # 加载配置文件
        model = build_detection_model(cfg)
        # 加载模型参数
        checkpoint = torch.load(self.args.model_file, map_location=torch.device("cpu"))

        self.show_visual_info = self.args.show_visual_info

        load_state_dict(model, checkpoint.pop("model"))

        # 将模型放置到GPU或者CPU上, 并设置为预测模式
        model.to(get_current_device())
        model.eval()
        # print(model)

        return model
```

---

+ 第3步: 参数解析函数 get_parser().

```plain
# 参数解析模块
    def get_parser(self):
        parser = argparse.ArgumentParser()

        parser.add_argument("--model_file", default="./detector/detectron_model_x152.pth", type=str, help="视觉检测的预训练模型文件")

        parser.add_argument("--config_file", default="./detector/detectron_model_x152.yaml", type=str, help="视觉检测配置文件")

        parser.add_argument("--show_visual_info", default=True, type=bool, help="显示视觉信息")

        # 检测后起始的下标
        parser.add_argument("--start_index", default=0, type=int, help="检测后的起始下标")
        # 检测后结束的下标
        parser.add_argument("--end_index", default=None, type=int, help="检测后的结束下标")
        # 设置batch_size的大小
        parser.add_argument("--batch_size", type=int, default=2, help="Batch size")
        # Faster RCNN 提取出来的特征个数, 默认100
        parser.add_argument("--num_features", type=int, default=100, help="提取的图像特征数量")
        # 输出的文件夹目录
        parser.add_argument("--output_folder", type=str, default="./output", help="特征输出的文件夹路径")
        # 读入的图像文件夹目录
        parser.add_argument("--image_dir", type=str, help="读入的图像文件夹目录")
        # 输出特征的哪一层
        parser.add_argument("--feature_name", type=str, help="提取的特征层名称", default="fc6")
        # 检测置信度阈值, 超过该阈值的方框将被选中
        parser.add_argument("--confidence_threshold", type=float, default=0, help="检测中被选中的方框的置信度阈值")
        # 当设置后, 模型将输出后台类的预测
        parser.add_argument("--background", action="store_true", help="模型将输出后台类的预测")
        return parser
```

---

+ 第4步: 图像处理函数 _image_transform().

```plain
# 图像格式变化的处理函数
    def _image_transform(self, path):
        img = Image.open(path)
        im = np.array(img).astype(np.float32)
        # 最后一个维度超过3通道时, 设置为RGB三通道模式
        if im.shape[-1] > 3:
            im = np.array(img.convert("RGB")).astype(np.float32)

        # IndexError: 灰度图像有太多的索引, 数组
        if len(im.shape) < 3:
            im = np.repeat(im[:, :, np.newaxis], 3, axis=2)

        # 图像转置的处理
        im = im[:, :, ::-1]
        im -= np.array([102.9801, 115.9465, 122.7717])
        im_shape = im.shape
        im_height = im_shape[0]
        im_width = im_shape[1]
        im_size_min = np.min(im_shape[0:2])
        im_size_max = np.max(im_shape[0:2])

        # 基于最小尺寸的比例
        im_scale = self.MIN_SIZE / im_size_min

        # 阻止的轴大于max_size, 如果更大, 就缩小它
        if np.round(im_scale * im_size_max) > self.MAX_SIZE:
            im_scale = self.MAX_SIZE / im_size_max

        # 对图片进行resize处理
        im = cv2.resize(im, None, None, fx=im_scale, fy=im_scale, interpolation=cv2.INTER_LINEAR)

        # 更改张量的维度
        img = torch.from_numpy(im).permute(2, 0, 1)

        # 返回宽和高
        im_info = {"width": im_width, "height": im_height}

        return img, im_scale, im_info
```

---

+ 第5步: 特征提取函数 _process_feature_extraction().

```plain
def _process_feature_extraction(self, output, im_scales, im_infos, feature_name="fc6", conf_thresh=0):
        # 获取输出proposals的侯选框
        batch_size = len(output[0]["proposals"])
        # 输出每一张图片的proposals的box长度
        n_boxes_per_image = [len(boxes) for boxes in output[0]["proposals"]]
        # 输出置信度的得分
        score_list = output[0]["scores"].split(n_boxes_per_image)
        # 对置信度缩放到0-1
        score_list = [torch.nn.functional.softmax(x, -1) for x in score_list]
        # 输出每一张图片中n个box的特征embedding
        feats = output[0][feature_name].split(n_boxes_per_image)

        cur_device = score_list[0].device
        feat_list = []
        info_list = []

        # 依次处理proposals中的候选框信息
        for i in range(batch_size):
            # 对输出的候选框除以缩放的尺度
            dets = output[0]["proposals"][i].bbox / im_scales[i]
            # 取到score_list中每一个score的得分
            scores = score_list[i]
            # max_conf 为最大置信度
            max_conf = torch.zeros(scores.shape[0]).to(cur_device)
            # 创建置信度过滤矩阵
            conf_thresh_tensor = torch.full_like(max_conf, conf_thresh)
            # 开始的index为1
            start_index = 1

            # 得分矩阵scores matrix的第0列, 对应于background类
            if self.args.background:
                start_index = 0

            for cls_ind in range(start_index, scores.shape[1]):
                cls_scores = scores[:, cls_ind]
                # 将IOU交并比小于0.5的去掉
                keep = nms(dets, cls_scores, 0.5)
                # 将比max_conf大的进行保留
                max_conf[keep] = torch.where((cls_scores[keep] > max_conf[keep])
                                            & (cls_scores[keep] > conf_thresh_tensor[keep]),
                                            cls_scores[keep],
                                            max_conf[keep])

            # 对类别概率分值和索引进行排序
            sorted_scores, sorted_indices = torch.sort(max_conf, descending=True)

            # box的数目
            num_boxes = (sorted_scores[: self.args.num_features] != 0).sum()

            # 每一个box的索引值
            keep_boxes = sorted_indices[: self.args.num_features]

            # 特征list添加
            feat_list.append(feats[i][keep_boxes])

            # bbox的得分
            bbox = output[0]["proposals"][i][keep_boxes].bbox / im_scales[i]

            # 使用得分预测类别标签
            objects = torch.argmax(scores[keep_boxes][:, start_index:], dim=1)
            # 将结果信息添加进列表中返回
            info_list.append({"bbox": bbox.cpu().numpy(),
                              "num_boxes": num_boxes.item(),
                              "objects": objects.cpu().numpy(),
                              "cls_prob": scores[keep_boxes][:, start_index:].cpu().numpy(),
                              "image_width": im_infos[i]["width"],
                              "image_height": im_infos[i]["height"]}
                            )

        return feat_list, info_list
```

---

+ 第6步: 映射字典函数 _category_id_convert_to_name().

```plain
# 类别id值到具体名称的映射字典函数
    def _category_id_convert_to_name(self, json_file):
        # 将配置信息的JSON文件读取并加载
        orig_json_file = open(json_file, encoding='utf-8')
        orig_json = json.load(orig_json_file)

        # 初始化映射字典, 并循环处理类别id和名称的映射关系赋值
        category_id_name_dict = {}
        for category_one in orig_json['categories']:
            category_id_name_dict[category_one['id']] = category_one['name']

        for category_one in orig_json['attCategories']:
            category_id_name_dict[category_one['id']] = category_one['name']

        return category_id_name_dict
```

---

+ 第7步: 主要处理函数 mainprocess().

```plain
def mainprocess(self, img_path, im_infos):
        # 对self.img_id_bboxes_attr_dict进行循环操作
        # 对每一个bbox标注
        # img1.size返回的宽度和高度(像素表示)
        img = Image.open(img_path, "r")
        draw = ImageDraw.Draw(img)
        category_id_name_dict = self._category_id_convert_to_name('./visual_genome_categories.json')
        # 提取所有bboxes信息
        im_info = im_infos[0]
        attr_bbox =  im_info["bbox"]
        for jdx, attr_dict in enumerate(attr_bbox):
            COLOR = COLOR_LIST[jdx % len(COLOR_LIST)]
            # 对每一个bboxes标注信息
            bbox = attr_dict 
            category_name = category_id_name_dict[im_info["objects"][jdx] ]
            x1, y1 = bbox[0], bbox[1]
            # 左上角坐标x1, y1
            top_left = (int(bbox[0]), int(bbox[1]))
            # 右上角坐标x2, y1
            top_right = (int(bbox[0])+int(bbox[2]), int(bbox[1]))
            # 右下角坐标x2, y2
            down_right = (int(bbox[0])+int(bbox[2]), int(bbox[1])+int(bbox[3]))
            # 左下角坐标x1, y2
            down_left = (int(bbox[0]), int(bbox[1])+int(bbox[3]))
            # 画线的坐标图, 5段线, 4顶点
            draw.line([top_left, top_right, down_right, down_left, top_left], width=5, fill=COLOR)
            # 将类别和分数写在左上角
            draw.text((x1 + 30, y1-FONT_SIZE), str(category_name), fill=COLOR)

        # 存储图片
        img_id = img_path.split('/')[-1]
        save_path = os.path.join('./', img_id + ".png")
        img.save(save_path, 'png')
```

---

+ 第8步: 检测特征函数 get_detectron_features().

```plain
# 获取检测的特征函数
    def get_detectron_features(self, image_paths):
        img_tensor, im_scales, im_infos = [], [], []
        for image_path in image_paths:
            # 图像tensor大小, 图像缩放尺度, 图像信息
            im, im_scale, im_info = self._image_transform(image_path)
            img_tensor.append(im)
            im_scales.append(im_scale)
            im_infos.append(im_info)

        current_img_list = to_image_list(img_tensor, size_divisible=32)
        current_img_list = current_img_list.to(get_current_device())
        print("current_img_list>>>> ", current_img_list)

        # 调用Faster RCNN检测模型得到输出结果
        with torch.no_grad():
            output = self.detection_model(current_img_list)
            # print('output info >>>', output )

        # 针对于检测模型的输出, 进行特征处理得到特征列表结果
        feat_list = self._process_feature_extraction(output, im_scales, im_infos, self.args.feature_name, self.args.confidence_threshold)

        return feat_list
```

---

+ 第9步: 存储图像特征函数 _save_feature().

```plain
def _save_feature(self, file_name, feature, info):
        file_base_name = os.path.basename(file_name)
        file_base_name = file_base_name.split(".")[0]
        info_file_base_name = file_base_name + "_info.npy"
        file_base_name = file_base_name + ".npy"

        np.save(os.path.join(self.args.output_folder, file_base_name), feature.cpu().numpy())
        np.save(os.path.join(self.args.output_folder, info_file_base_name), info)
```

---

+ 第10步: 提取特征函数 extract_features().

```plain
def extract_features(self):
        # 对待处理图像文件夹中每一个图像进行处理
        image_dir = self.args.image_dir
        # 处理单个文件
        if os.path.isfile(image_dir):
            print('output image_dir >>>> ', image_dir)
            features, infos = self.get_detectron_features([image_dir])
            print("infos >>> ", infos)
            # 显示视觉信息
            if self.show_visual_info:
                self.mainprocess(image_dir, infos)

            self._save_feature(image_dir, features[0], infos[0])
        # 处理文件夹的文件
        else:
            files = get_image_files(self.args.image_dir,
                                    exclude_list=self.args.exclude_list,
                                    start_index=self.args.start_index,
                                    end_index=self.args.end_index,
                                    output_folder=self.args.output_folder)

            finished = 0
            total = len(files)
            # 按照批次进行处理
            for chunk, begin_idx in chunks(files, self.args.batch_size):
                features, infos = self.get_detectron_features(chunk)

                for idx, file_name in enumerate(chunk):
                    self._save_feature(file_name, features[idx], infos[idx])

                finished += len(chunk)

                if finished % 200 == 0:
                    print(f"Processed {finished}/{total}")
```

---

+ 调用:

```plain
if __name__ == "__main__":
    # 实例化特征提取类对象
    feature_extractor = FeatureExtractor()
    # 调用类内提取特征的主逻辑函数
    feature_extractor.extract_features()
```

---

#### 第五步: 输出的检测图像结果.
+ 调用:

```plain
# 首先切换到该路径下
cd /home/multimodal/

# 然后执行提取特征的程序, 以参数形式传入一张待检测的图片
python extract_features_vmb.py --image_dir /home/multimodal/01235.png
```

---

+ 程序运行结束后, 随机摘取一张检测后的图片展示如下:

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/25755350/1730544066838-e82a3c56-b562-48fe-8206-d75e1d679a41.png)

---

---

### 小节总结
+ 学习视觉单元提取方法比对:
    - image-Region 相比对 image-Grid 方法的优势
+ Visual Genome 数据集的介绍
+ 处理仇恨言论检测图片的方法
    - 如何使用visual-genome 数据集进行模型训练
    - 使用训练好的模型对仇恨图片进行特征提取

---

---

---

---

  


> 来自: [2.2 FasterRCNN模型实现 - 多模态项目](http://127.0.0.1:5500/2.2%20FasterRCNN%E6%A8%A1%E5%9E%8B%E5%AE%9E%E7%8E%B0%20-%20%E5%A4%9A%E6%A8%A1%E6%80%81%E9%A1%B9%E7%9B%AE.html)
>

